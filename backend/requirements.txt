# ==============================================================================
# Albunmanía — Backend dependencies
# Pinned to versions validated in dev/staging (pip freeze 2026-05-15).
# Install: pip install -r requirements.txt
# ==============================================================================

# ------------------------------------------------------------------------------
# Django core
# ------------------------------------------------------------------------------
Django==6.0.4
asgiref==3.11.1
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-allauth[socialaccount]==65.16.1
django-cors-headers==4.9.0
django-cleanup==9.0.0
sqlparse==0.5.5

# ------------------------------------------------------------------------------
# Database
# ------------------------------------------------------------------------------
mysqlclient==2.2.8

# ------------------------------------------------------------------------------
# Production server
# ------------------------------------------------------------------------------
gunicorn==23.0.0

# ------------------------------------------------------------------------------
# Task queue (Huey + Redis)
# ------------------------------------------------------------------------------
huey==3.0.0
redis==7.4.0

# ------------------------------------------------------------------------------
# Media / images
# ------------------------------------------------------------------------------
pillow==12.2.0
easy-thumbnails==2.10.1

# ------------------------------------------------------------------------------
# Albunmanía — feature packages
# ------------------------------------------------------------------------------

# Google OAuth account-age verification (People API via requests)
requests==2.33.1
python-dateutil==2.9.0.post0

# Web Push (W3C standard, no Firebase)
pywebpush==2.3.0
py-vapid==1.9.4

# QR codes (Match QR presencial + listas compartibles)
qrcode[pil]==8.2

# Proximity match engine (haversine in-process)
haversine==2.9.0

# IP-based geolocation (MaxMind GeoLite2-City)
geoip2==4.8.1

# ------------------------------------------------------------------------------
# Admin / observability
# ------------------------------------------------------------------------------
django-silk==5.5.0
django-dbbackup==5.3.0

# ------------------------------------------------------------------------------
# Config
# ------------------------------------------------------------------------------
python-dotenv==1.2.2
typing_extensions==4.15.0

# ------------------------------------------------------------------------------
# Dev / testing (safe to install in all envs; no prod impact)
# ------------------------------------------------------------------------------
pytest==9.0.3
pytest-cov==7.1.0
pytest-django==4.12.0
coverage==7.13.5
factory-boy==3.3.3
Faker==40.15.0
freezegun==1.5.5
ruff==0.15.12
